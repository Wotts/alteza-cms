{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.getRoles }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_controller_index') }}">back to list</a>

    <a href="{{ path('user_controller_edit', {'id': user.id}) }}">edit</a>

    {{ include('user_controller/_delete_form.html.twig') }}
{% endblock %}
