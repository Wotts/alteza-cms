{% extends 'base.html.twig' %}

{% block title %}Role{% endblock %}

{% block body %}
    <div class="container">

        <h1>Role</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ role.id }}</td>
                </tr>
                <tr>
                    <th>Description</th>
                    <td>{{ role.description }}</td>
                </tr>
            </tbody>
        </table>

        <a class="btn btn-secondary" href="{{ path('role_index') }}">back to list</a>

        <a class="btn btn-secondary" href="{{ path('role_edit', {'id': role.id}) }}">edit</a>
    </div>
{% endblock %}
