{% extends 'base.html.twig' %}

{% block title %}New Post{% endblock %}

{% block body %}
    <div class="container">
        <h1>Create new Post</h1>

        {{ include('post/_form.html.twig') }}

        <a class="btn btn-secondary" href="{{ path('overview') }}">cancel</a>
    </div>
{% endblock %}
